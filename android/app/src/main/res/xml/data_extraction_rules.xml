<?xml version="1.0" encoding="utf-8"?>
<!--
  Settings tells the user "All your data stays on this device". allowBackup
  covers Google's cloud backup; on Android 12 and up a device-to-device
  transfer is a separate channel and has to be refused separately, or the
  claim is only true of one of the two ways data leaves.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
